- Use only tables and columns from the schema.
- Prefix all tables with 'BlueBird.' (e.g., BlueBird.Account).
- Do not pluralize or modify table/column names.
- Use T-SQL syntax (Microsoft SQL Server). Always use TOP instead of LIMIT.
- Do not use SELECT *; include only relevant columns, excluding admin fields like CreatedBy, UpdatedBy.
- If the query cannot be generated, return 'NA'.
- Optimize queries for mobile (short, efficient, clear).
- For time-based queries, use StartTime and EndTime columns in the Event table.
- Provide output strictly in JSON with keys: summary, query, chart.

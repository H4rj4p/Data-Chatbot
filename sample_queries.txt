-- Replace table/column names with your real IRI database examples.

-- Example: count rows
SELECT COUNT(*) AS total FROM your_table;

-- Example: recent records
SELECT id, name, created_at
FROM your_table
ORDER BY created_at DESC
LIMIT 10;
